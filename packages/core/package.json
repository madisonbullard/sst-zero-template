{
  "name": "@sst-zero-template/core",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "exports": {
    "./*.sql": "./src/*.sql.ts",
    "./*.ts": "./src/*.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "db": "bun sst shell drizzle-kit",
    "generate": "bun db generate",
    "migrate": "bun db migrate",
    "db:studio": "bun sst shell drizzle-kit studio",
    "generate:auth": "sst shell -- npx @better-auth/cli generate --config ./src/auth/index.ts --output .src/auth/better-auth.sql.ts",
		"typecheck": "tsc --noEmit",
		"zero:dev": "bun zero-cache-dev -p src/zero/index.ts"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
  "dependencies": {
    "@rocicorp/zero": "*",
    "better-auth": "1.1.17",
    "drizzle-orm": "^0.39.0",
    "nanoid": "^5.0.9"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.3"
  }
}
